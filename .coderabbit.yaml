# yaml-language-server: $schema=https://coderabbit.ai/integrations/schema.v2.json
language: 'ja-JP'

reviews:
  profile: 'chill'
  high_level_summary: true
  review_status: false

  auto_review:
    enabled: false
    drafts: false

  # CLI pre-review を主に使うため、PR 上の status は既定で出さない
  commit_status: false
  fail_commit_status: false

  # Walkthroughに "Prompt for AI Agents" を含める（Codexに渡しやすい）
  enable_prompt_for_ai_agents: true

  # ノイズになりやすいものだけ除外（! で除外）
  path_filters:
    - '!node_modules/**'
    - '!.next/**'
    - '!out/**'
    - '!build/**'
    - '!dist/**'
    - '!coverage/**'
    - '!playwright-report/**'
    - '!test-results/**'
    - '!backend/.venv/**'
    - '!backend/.ruff_cache/**'
    - '!backend/.pytest_cache/**'
    - '!**/__pycache__/**'
    - '!**/*.log'
